
/* this is a comment */
int x;				/* decl -> varDecl */
double n;
int w;
int y; int z;
void change(){

}
int testAll(int w, int z) {    /* decl -> fnDecl */
 int x;
 ++w;			       /* fnBody -> stmtList -> stmt */
 --w;
 w++;
 w--;
 scanf("%d", &w);
 scanf("%f", &z);
 printf("%d", 3 + 3);
 printf("%f", 4.0 * 12.2);
 printf("hello world");
 if (w == x) {
   int x;
   w = x;
 }
 if (w != z) {
   w = w / z;
 }
 else {                        /* nested conditionals, part of stmt */
   if (w == 0){
    w = w * z;
   }
 }
 while (x == y) {
  int w;
  w = 3;
}
w = 3 + 4 * 5 / 8 - 7;        /* stmt -> assign_exp -> muliple exp */
if (x < y) {                 /* more nested conditionals also does */
 int n;
if (x > z) {		     /* test of more exp */
   if (x <= w){
     x = w;
     if (z >= n) {
       double d;
       y = (w);
       x = 4;
       d = 5.23;
       s = "hello";
     }
   }
 }
}
if (y == -1){
  x = -1;
}
change();
return x;
}
